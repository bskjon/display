export interface Temperature {
    id: string,
    value: number,
    where: string,
    unit: string
}

export interface Humidity {
    id: string,
    value: number,
    where: string,
    unit: string
}

export interface Co2 {
    id: string,
    value: number,
    where: string,
    unit: string
}
